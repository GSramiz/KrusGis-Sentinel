# Веб-фреймворк
Flask==2.3.3
flask-cors==4.0.0

# Earth Engine API
earthengine-api==0.1.374

# Аутентификация для GEE
oauth2client==4.1.3

# Работа с environment variables
python-dotenv==1.0.0

# HTTP запросы
requests==2.31.0

# Вспомогательные (можно убрать если не нужны)
numpy==1.24.3
Pillow==10.0.0
