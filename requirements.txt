# Веб-фреймворк
Flask==2.3.3
Werkzeug==2.3.7

# Earth Engine API
earthengine-api==0.1.374

# Аутентификация (оставляем для GEE)
oauth2client==4.1.3

# Работа с environment variables
python-dotenv==1.0.0

# HTTP запросы (может пригодиться)
requests==2.31.0

# Вспомогательные
numpy==1.24.3
Pillow==10.0.0
